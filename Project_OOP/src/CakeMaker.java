public class CakeMaker extends Labratory {
    public CakeMaker(int x, int y, int capacity, String fule, String production, int output) {
        super(x, y, capacity, fule, production, output);
    }
}
